# This tells Lombok this is the root and it should stop looking in parent folders
config.stopBubbling = true

# This tells Lombok to copy the @Qualifier annotation to the generated constructor
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier